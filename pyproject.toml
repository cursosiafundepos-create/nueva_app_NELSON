[project]
name = "reportes-app"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "cursosiafundepos-create", email = "cursosiafundepos@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "flask>=3.1.3",
    "openpyxl>=3.1.5",
    "pandas>=3.0.5",
]

[project.scripts]
reportes-app = "reportes_app:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"
